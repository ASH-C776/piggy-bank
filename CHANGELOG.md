# 更新日志（CHANGELOG）

所有重要变更记录在此。版本号遵循 `主.次.修` 语义：

- **主版本**：数据结构/接口破坏性变更
- **次版本**：新功能模块
- **修订版本**：缺陷修复与体验优化

---

## 1.3.3（2026-09-17）

### Android 客户端
- **系统栏回实色暖白（#FFFDF8）**：1.3.1 的沉浸式透明系统栏在部分 MIUI 机型上会导致窗口打开/切后台动画明显掉帧，回退为实色方案；此前的顶部灰条问题一并消除。

### 飞牛 fnOS
- **首次提供 fpk 应用包**（`deploy/fpk/com.piggybank.app/`，fnpack v1.2.3 打包）：
  - Docker 镜像预构建（`piggy-bank:<版本>`），fpk 内不含数据
  - 数据挂载飞牛托管的应用数据目录（`${TRIM_PKGVAR}/data`），**升级覆盖安装不丢数据**
  - 应用中心手动安装，桌面图标直开浏览器入口（端口 3000）

---

## 1.3.0 – 1.3.2（2026-09-17）

### 成长记录 · 多孩子对比（R1）
- 家长端成长页新增**页面内对比模式**：多名孩子的身高曲线同图叠加，**X 轴按年龄对齐**（多孩子对比唯一公平基准）
- 纯对比逻辑抽为 `web/src/utils/growth-compare.ts`（色序/线型/剔除规则），`GrowthChart` 以 `series` 入参统一绘图；孩子端两个调用点零改动
- 对比模式刻意不画标准带（性别生日不同，画任何一条都会误导）、隐藏统计卡与历史记录
- 色槽按 child.id 升序固定，超过 4 名孩子用虚线二次编码

### 手机 UI 适配
- 成长页孩子选择 chip 去掉分数，只留头像+名字（省空间）
- 「对比全部」入口收成「对比」两字小胶囊（去掉图标与副行）

### Android
- 版本号 1.2.2 → 1.3.0（versionCode 6）
- 1.3.1 尝试沉浸式状态栏（透明系统栏 + edge-to-edge），因动画掉帧问题在 1.3.3 回退

---

## 1.2.0 – 1.2.2（2026-09-16）

### UI 全面改造（B0–B7）
- **Token 化**：颜色 34 + 圆角 8 档 + 字号 13 档 + 间距 16 档 + 动效 5 档，890 处硬编码清零
- **对比度达标**：强调色只做装饰不做小字文字色；文字用实色不用 alpha；渐变大字提供压暗达标 token（`--grad-num-*`）
- **字体本地自托管**：ZCOOL KuaiLe + Quicksand woff2 随应用分发，内网/NAS/APK 不再依赖 Google Fonts
- **弹窗收敛为一套**：`useDialog` 统一 Esc 关闭、Tab 焦点陷阱、全局打开栈、滚动锁引用计数；确认框初始焦点落在「取消」
- **可达性 + 移动端**：`:focus-visible`、`prefers-reduced-motion`、`100dvh` 回退行、`backdrop-filter` 透明度降级、玻璃卡可聚焦
- **底部导航换单色 SVG 图标**（`stroke="currentColor"`），选中态可点亮

---

## 1.0 – 1.1（基线）

- 家庭积分系统基线：日常项打卡、临时任务、手动加减分、积分商城、现金兑换、家长审核流（WebSocket 实时推送）
- 孩子端 / 家长端分离，孩子 PIN 登录、家长密码登录
- 身高体重成长曲线（对照中国儿童生长标准百分位）
- Capacitor 安卓客户端（hash 路由，WebView 兼容）
