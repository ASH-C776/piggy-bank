# Piggy Bank（存钱罐）部署教程

> 本教程面向没有技术基础的使用者，照着做即可。
> 全程只需要拷贝粘贴几条命令，不需要懂编程。

---

## 一、先确认你的设备满足条件

| 条件 | 怎么确认 |
|---|---|
| 一台 NAS（飞牛 / 群晖 / 威联通等） | 你已经有了 |
| NAS 上装了 **Docker** | 打开 NAS 的 Docker / 容器 应用，能看到界面即可 |
| NAS 能上网 | 能正常下载东西就行 |

---

## 二、需要准备的文件

一个压缩包：

```
piggy-bank-fixed.tar.gz   （约 740 KB）
```

把它传到 NAS 上任意一个文件夹里，**建议放在：**

```
/vol1/1000/download/
```

> 群晖用户可放在任意共享文件夹，例如 `/volume1/docker/`，下文路径相应替换即可。

---

## 三、开始部署（两种方法，选一种）

### 方法 A：命令行（推荐，最稳，不容易出错）

**第 1 步：打开 NAS 的终端**

飞牛 NAS：在「系统设置 → 终端 / SSH」里打开，或在 NAS 的「终端」应用里操作。

**第 2 步：解压**

把下面整段复制粘贴进去，然后按回车：

```bash
cd /vol1/1000/download
mkdir -p piggy-bank
tar xzf piggy-bank-fixed.tar.gz -C piggy-bank
cd piggy-bank
```

**第 3 步：给文件补上读取权限**

（这一步很重要，不做可能构建失败）

```bash
chmod -R u+rwX,go+rX .
```

**第 4 步：构建并启动**

```bash
docker compose up -d --build
```

> 这一步会下载依赖并编译，**第一次大约需要 3～10 分钟**，请耐心等待。
> 最后看到类似 `Container piggy-bank  Started` 就是成功了。

**第 5 步：确认运行**

```bash
docker ps
```

看到 `piggy-bank` 这一行、状态是 `Up` 就正常。

---

### 方法 B：图形界面（不想用命令的话）

以飞牛 NAS 为例：

1. 打开 NAS 的 **Docker / 容器** 应用
2. 找到 **「Compose」或「项目」** 功能，新建一个项目
3. 项目路径选择你解压后的文件夹（即含有 `docker-compose.yml` 的那个目录）
4. 应用/启动该项目

> 注意：图形界面各 NAS 叫法不同（群晖叫「容器管理器 → 项目」），只要选到那个目录并启动即可。

---

## 四、打开使用

在浏览器地址栏输入：

```
http://你的NAS的IP:3000
```

例如 NAS 的 IP 是 `192.168.1.50`，就访问：

```
http://192.168.1.50:3000
```

> 不知道 NAS 的 IP？一般在路由器管理页、或 NAS 的网络设置里能看到。

### 第一次登录（重要）

系统会自动创建一个管理账号：

| 项目 | 内容 |
|---|---|
| 用户名 | `admin` |
| 密码 | `admin123` |

**登录后请立刻修改密码**（在设置里改），默认密码不安全。

---

## 五、接下来怎么用

1. **用 `admin` 登录**（家长身份）
2. 在 **「孩子」/ 账号管理** 里，为每个孩子创建一个账号并设置 PIN 码
3. 在 **「奖惩」** 里设置加分项、扣分项、可兑换的奖励
4. 在 **「任务」** 里发布任务
5. 孩子用**自己的账号 + PIN** 登录，完成任务、申请兑换
6. 家长在 **「审核」** 里通过或拒绝孩子的申请

---

## 六、常见问题

### 1. 浏览器打不开？

- 确认服务在跑：`docker ps` 里 `piggy-bank` 状态是 `Up`
- 确认地址写对：`http://NAS的IP:3000`（注意是 `http` 不是 `https`）
- 确认手机/电脑和 NAS 连的是**同一个网络**

### 2. 构建时报错？

**先做这一步**（90% 的问题都是它）：

```bash
cd /vol1/1000/download/piggy-bank
chmod -R u+rwX,go+rX .
docker compose up -d --build
```

如果还报网络相关的错（例如 `ETIMEDOUT`、下载超时），说明网络不稳定，**重新执行一次**：

```bash
docker compose up -d --build
```

### 3. 端口 3000 被占用了？

编辑 `docker-compose.yml`，把这一行

```yaml
      - "3000:3000"
```

改成（例如换成 3100）

```yaml
      - "3100:3000"
```

保存后重新执行 `docker compose up -d`，然后访问 `http://NAS的IP:3100`。

### 4. 想停止 / 重启 / 删除？

```bash
cd /vol1/1000/download/piggy-bank

docker compose stop      # 停止
docker compose start     # 启动
docker compose restart   # 重启
docker compose down      # 停止并删除容器（数据保留）
```

### 5. 数据存在哪？会不会丢？

数据保存在项目目录下的 **`data`** 文件夹里（即 `/vol1/1000/download/piggy-bank/data`）。

- **升级程序时**：`data` 文件夹不会被删，数据保留
- **备份时**：直接复制整个 `data` 文件夹即可
- **千万注意**：不要删除 `data` 文件夹，否则账号和积分记录都会丢失

---

## 七、给别人（局域网内）访问

只要在同一个局域网内，其他手机/电脑直接用浏览器打开：

```
http://NAS的IP:3000
```

不需要在对方设备上装任何东西。

---

## 八、安全提醒

1. **务必修改默认密码**（`admin` / `admin123`）
2. 孩子的 PIN 码不要设得太简单
3. 不要把端口 3000 直接暴露到公网（除非你清楚如何做安全防护）

---

祝使用愉快！
