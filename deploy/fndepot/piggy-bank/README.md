# 🐷 存钱罐 · Piggy Bank

> 面向家庭的自托管积分管理系统 —— 让孩子在完成任务、赚取积分、兑换奖励的过程中，学会规划与储蓄。

**怎么玩**：孩子完成日常任务，或家长发布的临时任务 → 获得积分 → 用积分在商店兑换商品或现金；
所有申请由家长审核把关。**数据全部保存在你自己的 NAS 上（SQLite 单文件），不上传任何云端。**

---

## 功能特性

### 👨‍👩‍👧 家长端
- **总览**：所有孩子的积分、今日变化、待审核数量
- **积分**：设置日常加分/扣分项目（可限每日次数），一键为孩子加减分
- **任务**：发布临时任务（分类、积分奖励、截止时间）
- **审核**：审批任务完成、商品/现金兑换申请
- **商店**：管理可兑换商品（名称、所需积分、库存）
- **记录**：全部积分流水明细与操作人
- **成长**：记录身高体重，成长曲线对照中国儿童生长标准看百分位；支持多孩子按年龄对齐对比

### 🧒 孩子端
- **首页**：积分余额、生肖头像、等级进度
- **任务**：领取任务、提交完成，等待家长审核
- **商店**：用积分兑换商品，或申请兑换现金

### ✨ 其他
- 家长用账号 + 密码登录；孩子用账号 + PIN 码登录（无需记密码）
- 内置 12 生肖头像，WebSocket 实时推送（家长端审核数即时刷新）
- 响应式设计，手机 / 平板 / 电脑均可使用
- 可自定义兑换比例（如 10 积分 = 1 元）

---

## 安装说明

在飞牛应用中心 → 手动安装（或通过 FnDepot 安装）本应用包后：

1. 桌面出现「存钱罐」图标，点击即可打开
2. 默认管理员账号：`admin` / `admin123` —— **请登录后立刻在「设置」里改密码**
3. 在家长端添加孩子账号、设置孩子 PIN 码，即可开始使用

> 默认服务端口为 **3000**，如与现有应用冲突，可在应用设置里调整。
> 数据存放在系统托管的应用数据目录中，**升级、覆盖安装都不会丢数据**。

---

## 系统要求

- 飞牛 fnOS **1.2.0** 及以上
- x86_64 架构设备
- 无需额外依赖（应用自带运行环境）

---

## 常见问题

**安装后一直停在"启动中" / 提示拉取镜像失败？**

首次安装需要从 GitHub 容器仓库（ghcr.io）拉取约 210MB 的镜像，国内网络偶尔会慢或超时。可以：

1. 在飞牛「Docker → 设置 → 镜像加速」里配置一个可用的加速地址后重试
2. 或在应用中心里对「存钱罐」执行一次「停止 → 启动」，让 Docker 重试拉取
3. 已配置代理的用户，可在 Docker 的代理设置中填自己的代理地址

镜像地址：`ghcr.io/ash-c776/piggy-bank:1.3.3`（可在 Docker 里手动 `docker pull` 预拉取）

**端口冲突怎么办？** 默认占用 3000，如与本机其他应用冲突，可在应用设置里修改端口映射。

**数据存在哪？** 由飞牛托管在应用数据目录，升级、覆盖安装都不会丢；卸载时可按提示选择是否保留。

---

## 技术栈

| 层 | 技术 |
|---|---|
| 前端 | Vue 3 + Vite + Pinia |
| 后端 | Fastify + better-sqlite3 |
| 存储 | SQLite（单文件，零配置） |

---

## 开源与反馈

- 源码仓库：[gitee.com/kijeye/piggy-bank](https://gitee.com/kijeye/piggy-bank)
- 问题反馈：[Issues](https://gitee.com/kijeye/piggy-bank/issues)

本项目采用 **MIT License** 开源，可自由使用、修改、分发（含商业用途），保留版权声明即可。
